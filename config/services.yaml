parameters:
    app.posts_upload_dir: '%kernel.project_dir%/public/uploads/posts'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Service\PostMediaUploadService:
        arguments:
            $uploadDir: '%app.posts_upload_dir%'
            $projectDir: '%kernel.project_dir%'
