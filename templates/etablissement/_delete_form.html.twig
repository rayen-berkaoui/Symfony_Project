<form method="post" action="{{ path('app_etablissement_delete', {'idEtablissement': etablissement.idEtablissement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etablissement.idEtablissement) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
