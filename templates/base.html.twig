<!DOCTYPE html>
<html lang="fr" class="bg-black text-white h-full">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Tabaany Administration{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>T</text></svg>">
        
        <!-- Tailwind CSS -->
        <script src="https://cdn.tailwindcss.com"></script>
        
        <!-- Font Awesome -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        
        <!-- Custom Configuration for Black & Gold -->
        <script>
            tailwind.config = {
                theme: {
                    extend: {
                        colors: {
                            gold: { 400: '#FBBF24', 500: '#FFD700', 600: '#D4AF37' },
                            dark: { 100: '#2A2A2A', 200: '#1A1A1A', 300: '#121212', 400: '#0A0A0A' }
                        },
                        fontFamily: { sans: ['"Segoe UI"', 'Inter', '"Helvetica Neue"', 'sans-serif'] }
                    }
                }
            }
        </script>
        
        <style>
            body { background-color: #000000; color: #ffffff; }
            .card { background-color: rgba(10, 10, 10, 0.95); border: 1px solid rgba(255, 215, 0, 0.06); box-shadow: 0 8px 30px rgba(255, 215, 0, 0.06); border-radius: 16px; padding: 1.5rem; }
            .btn-gold { background-color: #FFD700; color: #000; transition: all 0.3s ease; font-weight: 600; border-radius: 8px; padding: 0.5rem 1.25rem; display: inline-block; }
            .btn-gold:hover { background-color: #D4AF37; box-shadow: 0 4px 15px rgba(255, 215, 0, 0.3); }
            .btn-dark { background-color: #1A1A1A; color: #FFD700; border: 1px solid rgba(255, 215, 0, 0.2); transition: all 0.3s ease; border-radius: 8px; padding: 0.5rem 1.25rem; display: inline-block; }
            .btn-dark:hover { background-color: #2A2A2A; border-color: #FFD700; }
            .input-modern { background-color: rgba(18, 18, 18, 0.9); border: 1px solid rgba(255, 215, 0, 0.08); border-radius: 10px; color: white; padding: 0.75rem 1rem; width: 100%; transition: border-color 0.3s; margin-bottom: 1rem; }
            .input-modern:focus { outline: none; border-color: rgba(255, 215, 0, 0.5); }
            label { display: block; color: rgba(255, 215, 0, 0.85); font-size: 0.75rem; font-weight: bold; letter-spacing: 0.05em; margin-bottom: 0.5rem; text-transform: uppercase; }
            table { width: 100%; border-collapse: separate; border-spacing: 0; }
            th { text-align: left; padding: 1rem; color: #FFD700; font-size: 0.875rem; text-transform: uppercase; letter-spacing: 0.05em; border-bottom: 1px solid rgba(255, 215, 0, 0.1); }
            td { padding: 1rem; border-bottom: 1px solid rgba(255, 215, 0, 0.05); color: #E5E5E5; }
            tr:hover td { background-color: rgba(255, 215, 0, 0.02); }
            
            /* form overrides */
            input[type="text"], input[type="number"], input[type="file"], select, textarea {
                background-color: rgba(18, 18, 18, 0.9) !important;
                border: 1px solid rgba(255, 215, 0, 0.08) !important;
                border-radius: 10px !important;
                color: white !important;
                transition: all 0.3s ease;
            }
            input:focus, select:focus, textarea:focus {
                border-color: rgba(255, 215, 0, 0.5) !important;
                outline: none !important;
                box-shadow: 0 0 0 1px rgba(255, 215, 0, 0.5), 0 0 10px rgba(255, 215, 0, 0.2) !important;
            }

            /* Custom scrollbar */
            ::-webkit-scrollbar { width: 8px; height: 8px; }
            ::-webkit-scrollbar-track { background: rgba(10, 10, 10, 0.8); }
            ::-webkit-scrollbar-thumb { background: rgba(255, 215, 0, 0.2); border-radius: 4px; }
            ::-webkit-scrollbar-thumb:hover { background: rgba(255, 215, 0, 0.5); }
        </style>

        {% block stylesheets %}{% endblock %}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        {% set frankenphpHotReload = app.request.server.get('FRANKENPHP_HOT_RELOAD') %}
        {% if frankenphpHotReload %}
        <meta name="frankenphp-hot-reload:url" content="{{ frankenphpHotReload }}">
        <script src="https://cdn.jsdelivr.net/npm/idiomorph"></script>
        <script src="https://cdn.jsdelivr.net/npm/frankenphp-hot-reload/+esm" type="module"></script>
        {% endif %}
    </head>
    <body class="flex h-screen overflow-hidden">
        
        <!-- Sidebar Navigation -->
        <aside class="w-64 bg-dark-400 border-r border-gold-500/10 flex-shrink-0 flex flex-col relative overflow-hidden backdrop-blur-xl bg-opacity-90 bg-gradient-to-b from-dark-400 to-black z-10 shadow-[4px_0_24px_rgba(255,215,0,0.05)]">
            <!-- decorative blur behind sidebar -->
            <div class="absolute -top-32 -left-32 w-64 h-64 bg-gold-500/5 rounded-full blur-3xl pointer-events-none"></div>

            <div class="h-20 flex items-center justify-center border-b border-gold-500/10 mt-4 mb-4 relative z-10 group">
                <h1 class="text-2xl font-black tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-gold-400 to-yellow-600 drop-shadow-[0_2px_10px_rgba(255,215,0,0.3)] transition-transform duration-300 transform group-hover:scale-105">
                    <i class="fa-solid fa-compass mr-2 text-gold-500 group-hover:animate-spin-slow"></i> TABAANY
                </h1>
            </div>
            <nav class="flex-1 overflow-y-auto py-6 relative z-10 custom-scrollbar">
                <ul class="space-y-3 px-4">
                    <li>
                        <a href="{{ path('app_home') }}" class="group flex items-center space-x-3 px-4 py-3.5 rounded-xl text-gray-400 hover:bg-gradient-to-r hover:from-gold-500/10 hover:to-transparent hover:text-gold-400 transition-all duration-300 relative overflow-hidden {% if app.request.attributes.get('_route') == 'app_home' %}bg-gradient-to-r from-gold-500/20 to-dark-200/50 text-gold-400 shadow-[0_0_15px_rgba(255,215,0,0.1)] border-l-2 border-gold-400{% endif %}">
                            <i class="fa-solid fa-chart-pie w-6 text-center text-lg transition-transform group-hover:scale-110"></i>
                            <span class="font-semibold tracking-wide">Vue d'ensemble</span>
                            {% if app.request.attributes.get('_route') == 'app_home' %}<div class="absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-gold-500/20 to-transparent"></div>{% endif %}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_lieu_touristique_index') }}" class="group flex items-center space-x-3 px-4 py-3.5 rounded-xl text-gray-400 hover:bg-gradient-to-r hover:from-gold-500/10 hover:to-transparent hover:text-gold-400 transition-all duration-300 relative overflow-hidden {% if app.request.attributes.get('_route') starts with 'app_lieu_touristique' %}bg-gradient-to-r from-gold-500/20 to-dark-200/50 text-gold-400 shadow-[0_0_15px_rgba(255,215,0,0.1)] border-l-2 border-gold-400{% endif %}">
                            <i class="fa-solid fa-map-location-dot w-6 text-center text-lg transition-transform group-hover:scale-110"></i>
                            <span class="font-semibold tracking-wide">Lieux Touristiques</span>
                            {% if app.request.attributes.get('_route') starts with 'app_lieu_touristique' %}<div class="absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-gold-500/20 to-transparent"></div>{% endif %}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_categorie_index') }}" class="group flex items-center space-x-3 px-4 py-3.5 rounded-xl text-gray-400 hover:bg-gradient-to-r hover:from-gold-500/10 hover:to-transparent hover:text-gold-400 transition-all duration-300 relative overflow-hidden {% if app.request.attributes.get('_route') starts with 'app_categorie' %}bg-gradient-to-r from-gold-500/20 to-dark-200/50 text-gold-400 shadow-[0_0_15px_rgba(255,215,0,0.1)] border-l-2 border-gold-400{% endif %}">
                            <i class="fa-solid fa-layer-group w-6 text-center text-lg transition-transform group-hover:scale-110"></i>
                            <span class="font-semibold tracking-wide">Catégories</span>
                            {% if app.request.attributes.get('_route') starts with 'app_categorie' %}<div class="absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-gold-500/20 to-transparent"></div>{% endif %}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_adresse_index') }}" class="group flex items-center space-x-3 px-4 py-3.5 rounded-xl text-gray-400 hover:bg-gradient-to-r hover:from-gold-500/10 hover:to-transparent hover:text-gold-400 transition-all duration-300 relative overflow-hidden {% if app.request.attributes.get('_route') starts with 'app_adresse' %}bg-gradient-to-r from-gold-500/20 to-dark-200/50 text-gold-400 shadow-[0_0_15px_rgba(255,215,0,0.1)] border-l-2 border-gold-400{% endif %}">
                            <i class="fa-solid fa-location-dot w-6 text-center text-lg transition-transform group-hover:scale-110"></i>
                            <span class="font-semibold tracking-wide">Adresses</span>
                            {% if app.request.attributes.get('_route') starts with 'app_adresse' %}<div class="absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-gold-500/20 to-transparent"></div>{% endif %}
                        </a>
                    </li>
                </ul>
            </nav>
            
            <div class="mt-auto p-4 border-t border-gold-500/10 relative z-10">
                <div class="bg-gradient-to-br from-dark-200 to-black border border-gold-500/20 p-4 rounded-xl flex items-center gap-3 shadow-[0_4px_15px_rgba(0,0,0,0.5)] transform hover:-translate-y-1 transition-transform cursor-pointer">
                    <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-gold-400 to-amber-600 flex items-center justify-center text-black font-bold shadow-inner">
                        A
                    </div>
                    <div class="flex-1 overflow-hidden">
                        <p class="text-white font-bold text-sm truncate">Admin Tabaany</p>
                        <p class="text-gold-500/70 text-xs truncate">Super Admin</p>
                    </div>
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 overflow-y-auto bg-[#050505] flex flex-col relative w-full h-full shadow-inner scroll-smooth">
            <!-- Background Glows -->
            <div class="absolute top-0 right-0 w-96 h-96 bg-gold-500/5 rounded-full blur-[100px] pointer-events-none"></div>
            <div class="absolute bottom-0 left-0 w-96 h-96 bg-blue-500/5 rounded-full blur-[100px] pointer-events-none"></div>
            <!-- Top Header -->
            <header class="bg-dark-300/80 backdrop-blur-md border-b border-gold-500/10 px-8 py-4 flex justify-between items-center sticky top-0 z-50">
                <div class="flex items-center text-gray-400 text-sm font-medium">
                    <i class="fa-solid fa-home mr-2 text-gold-500"></i>
                    {% block breadcrumb %}
                        <span>Administration</span>
                    {% endblock %}
                </div>
                <div class="flex items-center gap-6">
                    <button class="text-gray-400 hover:text-gold-400 transition-colors relative">
                        <i class="fa-solid fa-bell text-lg"></i>
                        <span class="absolute -top-1 -right-1 flex h-3 w-3">
                            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-gold-400 opacity-75"></span>
                            <span class="relative inline-flex rounded-full h-3 w-3 bg-gold-500"></span>
                        </span>
                    </button>
                    <div class="flex items-center gap-3 cursor-pointer group">
                        <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-gold-500 to-amber-700 flex items-center justify-center text-black font-bold border-2 border-transparent group-hover:border-white transition-all">
                            A
                        </div>
                        <div class="hidden md:block text-sm">
                            <p class="text-white font-semibold group-hover:text-gold-400 transition-colors">Admin Tabaany</p>
                            <p class="text-gray-500 text-xs">admin@tabaany.com</p>
                        </div>
                        <i class="fa-solid fa-chevron-down text-gray-500 text-xs group-hover:text-gold-400"></i>
                    </div>
                </div>
            </header>

            <div class="p-8 max-w-7xl mx-auto w-full flex-1 z-10">
                <!-- Flash Messages Overlay -->
                <!-- Add fixed container for toasters top right -->
                <div class="fixed top-24 right-8 z-[100] flex flex-col gap-3 min-w-[320px] max-w-sm pointer-events-none">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="pointer-events-auto p-4 rounded-xl flex items-start gap-4 bg-dark-200/90 backdrop-blur-md border-l-4 shadow-[0_8px_30px_rgba(0,0,0,0.5)] transform transition-all duration-500 hover:scale-[1.02] border-{% if label == 'success' %}green-500{% elseif label == 'danger' %}red-500{% elseif label == 'warning' %}yellow-500{% else %}blue-500{% endif %} animate-[slide-in-right_0.4s_ease-out]">
                            <i class="fa-solid mt-1 {% if label == 'success' %}fa-check-circle text-green-500{% elseif label == 'danger' %}fa-exclamation-circle text-red-500{% else %}fa-info-circle text-blue-500{% endif %} text-xl drop-shadow"></i>
                            <div class="text-gray-200 font-medium text-sm flex-1">{{ message }}</div>
                            <button onclick="this.parentElement.style.opacity='0'; setTimeout(() => this.parentElement.remove(), 300)" class="ml-auto text-gray-500 hover:text-white transition-colors">
                                <i class="fa-solid fa-xmark"></i>
                            </button>
                        </div>
                    {% endfor %}
                {% endfor %}
                </div>

                <style>
                    @keyframes slide-in-right {
                        0% { transform: translateX(100%); opacity: 0; }
                        100% { transform: translateX(0); opacity: 1; }
                    }
                </style>

                {% block body %}{% endblock %}
            </div>
            
            <!-- Footer -->
            <footer class="mt-auto border-t border-gold-500/10 py-6 px-8 text-center text-gray-500 text-sm">
                <p>&copy; {{ 'now'|date('Y') }} Tabaany Administration. Tous droits réservés.</p>
            </footer>
        </main>
        
    </body>
</html>
